cyber_risk:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth  # <--- CRITICAL: Uses the Cloud Build robot's own identity
      project: cyberai-480816
      dataset: raw_layer
      threads: 4
      timeout_seconds: 300
      location: US