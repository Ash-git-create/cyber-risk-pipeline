version: 2

sources:
  - name: raw_layer
    database: "{{ env_var('GCP_PROJECT_ID', 'cyberai-480816') }}"
    schema: raw_layer
    tables:
      - name: nvd_cves
      - name: assets
      - name: cisa_kev
      - name: otx_intel
